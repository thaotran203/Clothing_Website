{% extends 'base.html.twig' %}

{% block title %}Edit Cart{% endblock %}

{% block body %}
    <h1>Edit Cart</h1>

    {{ include('cart/_form.html.twig', {'button_label': 'Update'}) }}
    
    <a class="btn btn-dark" href="{{ path('app_review_cart') }}">back to list</a>
    
    {{ include('cart/_delete_form.html.twig') }}
{% endblock %}
